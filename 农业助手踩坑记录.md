# 农业助手 FreeRTOS 任务优先级 & 内存管理 踩坑记录

> 硬件：ESP32-S3 + Octal PSRAM
> 项目：农业物联网网关（WiFi/CAT1/LoRa/MQTT/LVGL）
> 日期：2026-06-23

---

## 1. PowerShell 写文件导致中文乱码

**现象：**
```
warning: missing terminating " character
error: unterminated argument list invoking macro "ESP_LOGI"
```
文件中的中文字符串全部变成 `閸掑洦宕` 等乱码。

**原因：** 用 `Set-Content -NoNewline` 写 UTF-8 源文件时，PowerShell 默认用系统编码（Windows 中文环境为 GB2312）写入，导致 UTF-8 中文全部损坏。

**修复：** 改用 `[System.IO.File]::ReadAllText` / `[System.IO.File]::WriteAllText` 并**显式指定 UTF-8 编码**：
```powershell
$content = [System.IO.File]::ReadAllText("file.c", [System.Text.Encoding]::UTF8)
# ... 修改 $content ...
[System.IO.File]::WriteAllText("file.c", $content, [System.Text.Encoding]::UTF8)
```

**教训：** 在中文 Windows 下操作含中文的源文件，**禁止使用 `Set-Content`**，必须显式指定 UTF-8 编码。

**文件：** `main/main.c`、`components/bsp_uart/wifi_manager.c`

---

## 2. 删除 if 块后遗留多余的 `}` 导致函数提前结束

**现象：**
```
error: expected declaration specifiers or '...' before '(' token
error: unknown type name 'ctx'
error: expected identifier or '(' before 'do'  (ESP_LOGI 宏展开)
```
编译错误集中在某个函数中间，所有后续代码被误认为在文件作用域。

**原因：** 将 `calloc` 改为静态分配时，删除了 NULL 检查块但保留了 `if` 块的闭合 `}`：
```c
// 删除前
ota_check_ctx_t *ctx = calloc(1, sizeof(*ctx));
if (ctx == NULL) {          // ← 删除了 if 行
    ESP_LOGE(TAG, "...");
    return;
}                            // ← 但这个 } 没删！
```

这个孤立的 `}` 提前关闭了函数体，导致后续所有代码跑到函数外面。

**修复：** 删除多余的 `}`。

**文件：** `components/bsp_uart/wifi_cat1.c` 行 ~1572

---

## 3. `#pragma GCC diagnostic` 对 `-Wformat-truncation` 无效

**现象：** 用 `#pragma GCC diagnostic push / ignored "-Wformat-truncation" / pop` 包裹 `snprintf` 调用后，编译仍然报同样的 `-Werror=format-truncation=` 错误。

**原因：** ESP-IDF 使用的 `xtensa-esp-elf-gcc` 对 `-Wformat-truncation` 的 pragma 支持不完整，源代码级抑制不生效。

**修复：** 在组件 `CMakeLists.txt` 中用 `set_source_files_properties` 对**单个文件**抑制：
```cmake
set_source_files_properties(wifi_cat1.c PROPERTIES COMPILE_FLAGS -Wno-format-truncation)
```
比全局 `-Wno-format-truncation` 更精准，不影响其他文件的告警检测。

**文件：** `components/bsp_uart/CMakeLists.txt`

---

## 4. `snprintf` 缓冲区大小编译时误报

**现象：**
```
error: '%s' directive output may be truncated writing up to 1451 bytes
       into a region of size 463 [-Werror=format-truncation=]
note: 'snprintf' output between 60 and 1682 bytes into a destination of size 512
```

**原因：** 编译器静态分析时取 `%s` 对应缓冲区的**声明大小**作为最大可能值。例如 `char encoded_device_name[256]` 会被认为可能输出 256 字节，加上 URL 模板固定部分，理论最大值 1682 > 实际缓冲区 512。

但实际 OneNET API 返回的 URL + 设备名通常不超过 400 字节，远小于 512。

**教训：** `-Wformat-truncation` 是 GCC 7+ 引入的静态分析，对 `snprintf` + `%s` 组合容易产生误报。处理顺序：
1. 先确认实际数据是否真的会超 → 不会
2. 尝试 `#pragma GCC diagnostic` → 无效
3. CMake 层级 `set_source_files_properties` 抑制 → 有效

**文件：** `components/bsp_uart/wifi_cat1.c`

---

## 5. FreeRTOS 任务优先级全部扎堆同一级别

**现象：** 项目中 15 个 FreeRTOS 任务，11 个集中在优先级 5，彼此之间只是时间片轮转而非抢占调度。LoRa 实时轮询和 LVGL 屏幕刷新在同等优先级竞争 CPU。

**根因：** 初始开发时所有 `xTaskCreate` 几乎都用默认优先级 5，未按业务关键程度分层。

**修复：** 按四级重新分配：

| 优先级 | 含义 | 任务 |
|--------|------|------|
| **6** | 实时通信 | `lora_poll_task` — 子设备数据必须实时 |
| **5** | 核心感知+网络 | `iic_sensor_task`、`sensor_upload`、`mqtt_start`、`ota_boot` |
| **4** | 备用链路+管理 | `cat1_task`、`at_mqtt_task`、`cat1_delay`、`captive_dns`、`sub_login_retry` |
| **3** | UI + 后台 | `lvgl`（屏幕刷新延迟人眼不可感知）、`ota_notify`×2、`wifi_cleanup`、`cat1_off` |

**关键决策：**
- `lvgl` 从 5 降到 3 — 屏幕刷新延迟几十毫秒人眼无感，不应抢占传感器上传
- `ota_boot` 从 3 升到 5 — OTA 固件检查不应被低优先级任务阻塞
- `lora_poll_task` 升到 6（最高）— 实时子设备通信

**文件：** `main/main.c`、`components/bsp_uart/wifi_manager.c`

---

## 6. 内存管理精细化改造

**背景：** 项目原始内存策略为纯动态 `malloc/calloc/free`，所有分配走 ESP-IDF 默认堆（内部 DRAM + PSRAM 透明整合），无静态预分配、无内存池、无 OTA 内存保护。

### 6.1 堆水位监控

新增 `heap_monitor_task`（优先级 2），每 60 秒上报：
- 内部 DRAM 空闲 / 最小空闲
- PSRAM 空闲 / 最小空闲 
- 关键任务（`mqtt_start`、`ota_boot`）的栈高水位

日志示例：
```
HEAP: DRAM free=245760 min=210944 | SPIRAM free=8388608 min=8126464
HEAP: mqtt_start stack HWM=2340
HEAP: ota_boot   stack HWM=5140
```

**文件：** `main/main.c`

### 6.2 大缓冲定向到 PSRAM

将 OTA 下载缓冲、HTTP 响应缓冲、OTA 分片等大块分配改用 `heap_caps_malloc(MALLOC_CAP_SPIRAM)`，释放内部 DRAM 给 WiFi/LWIP 协议栈：

| 分配点 | 大小 | 原方案 | 新方案 |
|--------|------|--------|--------|
| OTA 下载缓冲 | 4KB | `malloc` | `heap_caps_malloc(SPIRAM)` |
| HTTP 响应缓冲 | 可变 | `malloc` | `heap_caps_malloc(SPIRAM)` |
| OTA 分片 | 16B+数据 | `malloc` | `heap_caps_malloc(SPIRAM)` |

每个都加了 **INTERNAL 回退**：SPIRAM 分配失败时自动尝试内部 RAM。

**文件：** `components/bsp_uart/wifi_cat1.c`

### 6.3 高频小对象静态化

消除重复 `malloc/free`，减少堆碎片：

| 对象 | 原方案 | 新方案 |
|------|--------|--------|
| AT 命令缓冲 (256B) | `malloc(256)` + `free()` | `static uint8_t at_cmd_buf[256]` |
| OTA 检查上下文 (~1KB) | `calloc(1, sizeof(*ctx))` + `free(ctx)` | `static ota_check_ctx_t ota_ctx_storage` + `memset` |

**文件：** `components/bsp_uart/wifi_cat1.c`


### 6.4 没有内存池会遇到什么问题

OTA 下载 100 个分片时，每片都 malloc(sizeof(chunk) + dataLen) → 写入 Flash → free(chunk)。高频 malloc/free 的后果：

```text
第 1 片:  [chunk 1KB][...................空闲..................]
第 10 片: [chunk 1KB][LVGL 帧缓冲 10KB][MQTT buf 2KB][空闲...]
第 11 片: malloc → 找不到连续 1KB → 跳到更后面分配
           [旧chunk空洞1KB][LVGL 10KB][MQTT 2KB][chunk11 1KB][空..]
第 50 片: [空洞][空洞][LVGL][空洞][空洞][MQTT][空洞][chunk][空洞][空洞]...
          ↑ 碎片遍布整个堆，总空闲 80KB 但没有连续 1KB
第 51 片: malloc → NULL → 假性 OOM 崩溃
```

**具体后果：**
- 明明 heap_caps_get_free_size 显示还有 50-80KB 空闲，但 malloc 返回 NULL
- 原因：空闲内存碎片化，没有连续空间放下新 chunk
- 假性 OOM —— 空闲总量够但没有连续块
- 在生产环境极难复现和诊断（取决于 OTA 期间其他任务的时序）
- 即使 OTA 成功完成，碎片残留也会影响后续 LVGL/MQTT 的分配

### 6.5 OTA 内存门槛

在 `esp_ota_begin` 前增加堆检查：

```c
uint32_t free_int = heap_caps_get_free_size(MALLOC_CAP_INTERNAL);
uint32_t free_spi = heap_caps_get_free_size(MALLOC_CAP_SPIRAM);
if (free_int < 64*1024 || free_spi < 128*1024) {
    return ESP_ERR_NO_MEM;  // 拒绝 OTA，避免下载中途 OOM 变砖
}
```

阈值：内部 DRAM ≥ 64KB + PSRAM ≥ 128KB。

**文件：** `components/bsp_uart/wifi_cat1.c`

---

## 总结

| 踩坑 | 类型 | 教训 |
|------|------|------|
| PowerShell 写中文文件乱码 | 工具 | Windows 下必须显式指定 UTF-8 编码读写 |
| if 块删除遗留 `}` | 逻辑 | 删除代码块时检查配对括号 |
| `#pragma GCC diagnostic` 无效 | 编译器 | ESP-IDF 的 gcc 对某些 pragma 支持不完整，用 CMake 层级抑制 |
| `-Wformat-truncation` 误报 | 编译器 | snprintf + %s 容易触发，确认实际数据小于缓冲区后抑制 |
| 任务优先级全扎堆 5 | 设计 | 按业务关键度分层，保障实时任务不受 UI 干扰 |
| 纯动态分配无保护 | 设计 | 大缓冲走 PSRAM、小对象静态化、OTA 前检查堆水位 |

---

## 5. OTA 管理器拆分 & 内存池优化

**背景：**
原 `wifi_cat1.c` 文件过于庞大（~2200行），OTA 相关逻辑和 CAT1 AT 命令混杂。同时 OTA 下载期间每个 `OTA_ZC_Chunk` 独立 malloc/free，高频分配释放导致堆碎片。

**改动：**

### 5.1 文件拆分
将 OTA 相关代码从 `wifi_cat1.c` 提取到 `components/bsp_uart/ota_manager.c` + `ota_manager.h`：

| 模块 | 文件 | 职责 |
|------|------|------|
| CAT1 AT 命令 | `wifi_cat1.c` | GPIO 初始化、AT 命令收发、MQTT 连接 |
| OTA 子系统 | `ota_manager.c` | OneNET FuseOTA、HTTP 流式 OTA、ZC 分片 OTA、版本上报 |
| WiFi 管理 | `wifi_manager.c` | WiFi 连接、数据上报 |

### 5.2 OTA 内存池（预分配，零碎片）
```c
#define OTA_ZC_CHUNK_DATA_MAX   1024
#define OTA_ZC_POOL_SIZE        OTA_ZC_QUEUE_LEN

void ota_zc_pool_init(void);
OTA_ZC_Chunk *ota_zc_pool_acquire(uint16_t datalen);
void ota_zc_pool_release(OTA_ZC_Chunk *chunk);
```

优先使用 PSRAM（`MALLOC_CAP_SPIRAM`），不够再 fallback 到内部 DRAM。OTA 期间全程无 free，避免堆碎片。

### 5.3 踩坑：拆分后中文注释/日志全部乱码

**现象：**
提取后的 `ota_manager.c` 中所有中文日志变成：
- `"OTA log message"` 占位符（12处）
- `"fuse-ota 鏌ヨ type=%d..."` 等乱码
- 格式字符串断裂 `"version=%s (no pending upgrade, "` → 编译报错

**原因：**
拆分时 PowerShell/编辑器编码处理不当，UTF-8 中文在多次复制粘贴中损坏。

**修复（17处）：**
- 11处 `"OTA log message"` 占位符 → 从 `_original_wifi_cat1.c` 还原为正确中文
- 4处乱码中文字符串 → 还原
- 1处格式字符串断裂 → 还原
- 1处缺分号/参数 → 补全

### 5.4 踩坑：重复 if 块 + 大括号不平衡

**现象：** `WiFi_Cat1_ReportBootOtaResult` 函数中：
```c
if (mark_err == ESP_OK) {
if (mark_err == ESP_OK) {  // ← 重复行，第一行无 body
    ESP_LOGW(TAG, "Failed to mark OTA image valid: %s", ...);
    return;
}
// 还缺一个 } 闭合外层 if (running != NULL)
```

**修复：** 删除重复行，补全 if/else 和缺失的闭合大括号（11/11 平衡）。

### 5.5 踩坑：CMakeLists.txt 链接器标志误放入 COMPILE_FLAGS

```cmake
# 错误：-Wl,--undefined= 是链接器标志
set_source_files_properties(ota_manager.c PROPERTIES COMPILE_FLAGS
    "-Wl,--undefined=WiFi_Cat1_PropertyVersion ...")
```

**修复：** 删除此行。被标记的符号在其他 .c 文件中已有调用，链接器自动解析。

### 5.6 wifi_cat1.h 头文件注释全部乱码
28处中文注释变为乱码，从备份还原。

---

## 总结（更新）

| 踩坑 | 类型 | 教训 |
|------|------|------|
| PowerShell 写中文文件乱码 | 工具 | Windows 下必须显式指定 UTF-8 编码读写 |
| if 块删除遗留 `}` | 逻辑 | 删除代码块时检查配对括号 |
| `#pragma GCC diagnostic` 无效 | 编译器 | ESP-IDF 用 CMake 层级抑制 |
| 任务优先级全扎堆 5 | 设计 | 按业务关键度分层 |
| 纯动态分配无保护 | 设计 | 大缓冲走 PSRAM、小对象静态化 |
| **OTA 文件拆分中文全损坏** | 编码 | 跨文件移动代码时保留原始编码，用备份对比还原 |
| **重复 if 块 + 大括号不平衡** | 逻辑 | 重构后逐函数检查大括号配对 |
| **链接器标志误放 COMPILE_FLAGS** | CMake | `-Wl,` 前缀是链接器标志 |
| **OTA 内存池零碎片** | 设计 | 预分配池化：一次 malloc、多次使用、永不 free |
| **无系统级降级策略** | 设计 | mem_guard 四级降级：预警→关日志→停LVGL/OTA/MQTT→有序重启 |

---

## 6. 低内存紧急降级策略 (mem_guard)

> 日期：2026-06-26
> 组件：`components/mem_guard/`


### 6.1 没有降级策略会遇到什么问题

ESP32-S3 可用 DRAM 约 300-400KB。项目同时驱动 LVGL 屏幕、WiFi + MQTT、LoRa 轮询、传感器采集、HTTP OTA。堆耗尽通常发生在 **OTA 下载 + LVGL 刷新 + MQTT 收发 + LoRa 轮询同时发生** 时：

```text
malloc(OTA chunk) → 成功
malloc(LVGL buffer) → 成功
malloc(MQTT payload) → 返回 NULL ← 堆耗尽

→ ESP_LOGE("...") → 内部尝试 malloc → 也返回 NULL
→ 空指针解引用 → Guru Meditation Error / LoadProhibited
→ Watchdog 超时 → 硬重启
→ 重启后没有日志留下来，根本不知道崩在哪里
```

**具体后果：**
- 日志里只有 malloc returned NULL 或什么都没有 —— 崩溃点在日志刷出之前
- 崩在 OTA 写入 Flash 中途 → 分区损坏 → 变砖，需要 USB 重烧
- 崩在 MQTT 发送中途 → 服务器以为设备离线 → 误报故障
- 用户看到：屏幕突然黑掉，重启，过一会又黑掉

### 6.2 解决思路

项目运行在 ESP32-S3 上，同时驱动 LVGL 屏幕、WiFi + MQTT、LoRa 轮询、传感器采集、OTA 升级等多个子系统。在极端场景（如 OTA 下载 + LVGL 渲染同时发生）下，堆可能被耗尽，malloc 返回 NULL 后直接崩溃，无任何恢复机会。

项目运行在 ESP32-S3 上，同时驱动 LVGL 屏幕、WiFi + MQTT、LoRa 轮询、传感器采集、OTA 升级等多个子系统。在极端场景（如 OTA 下载 + LVGL 渲染同时发生）下，堆可能被耗尽，`malloc` 返回 NULL 后直接崩溃，无任何恢复机会。

### 6.3 设计思路

不是等 `malloc` 返回 NULL 再崩溃，而是在堆低于阈值时**主动降级**：

| 级别 | 阈值 (DRAM 空闲) | 清除阈值 | 动作 |
|------|-----------------|---------|------|
| NORMAL | > 60 KB | — | 全部正常运行 |
| WARN | ≤ 50 KB | > 60 KB | 抑制非关键日志 |
| CRITICAL | ≤ 30 KB | > 40 KB | 暂停 LVGL 刷新、拒绝 OTA、禁止新 MQTT 订阅 |
| EMERGENCY | ≤ 15 KB | > 25 KB | 3 秒后有序 `esp_restart()` |

关键设计：
- **滞回（hysteresis）**：恢复需要超过"清除阈值"而非原始进入阈值，避免抖动
- **有序重启**：EMERGENCY 级别等 3 秒（让日志刷出），然后调用 `esp_restart()`，而不是等下次 malloc 失败时随机崩溃
- **安全检查点**：不修改内核，只在各子系统的关键路径上添加守卫

### 6.4 新组件结构

```
components/mem_guard/
├── CMakeLists.txt
├── include/
│   └── mem_guard.h      # 公共 API
└── mem_guard.c           # 实现
```

**公共 API：**

| 函数 | 用途 |
|------|------|
| `mem_guard_init()` | 初始化并启动 2s 周期监控任务（优先级 7，最高） |
| `mem_guard_get_level()` | 返回当前降级级别 |
| `mem_guard_level_at_least(level)` | 判断是否达到或超过指定级别 |
| `mem_guard_lvgl_paused()` | 便捷函数：LVGL 是否应暂停 |
| `mem_guard_ota_blocked()` | 便捷函数：OTA 是否应拒绝 |
| `mem_guard_mqtt_sub_blocked()` | 便捷函数：MQTT 订阅是否应禁止 |
| `mem_guard_log_suppressed()` | 便捷函数：非关键日志是否应抑制 |
| `mem_guard_check_now()` | 手动触发一次堆检查（大分配前） |
| `mem_guard_malloc(size, label)` | 安全 malloc：EMERGENCY 级直接返回 NULL |
| `mem_guard_free(ptr)` | 对应的释放 |

### 6.5 集成点

**main.c：**
- `app_main()` 末尾调用 `mem_guard_init()`（在 LVGL、WiFi、MQTT 全启动之后）
- `lvgl_task` 循环首行检查 `mem_guard_lvgl_paused()`，暂停时 sleep 500ms
- `wifi_state_callback` 创建 MQTT/OTA 任务前检查 `!mem_guard_level_at_least(MEM_LEVEL_CRITICAL)`
- `heap_monitor_task` 日志输出前检查 `mem_guard_log_suppressed()`

**esp_mqtt.c：**
- `MQTT_EVENT_CONNECTED` 中的 subscribe 循环被 `mem_guard_mqtt_sub_blocked()` 包裹

**ota_manager.c：**
- `WiFi_Cat1_StartOTA()` 入口第一行检查 `mem_guard_ota_blocked()`

### 6.6 可配置阈值

所有阈值通过 `#define` 宏在 `mem_guard.h` 顶部定义，编译前可调整：

```c
#define MEM_GUARD_WARN_THRESHOLD       (50 * 1024)   //  50 KB
#define MEM_GUARD_CRITICAL_THRESHOLD   (30 * 1024)   //  30 KB
#define MEM_GUARD_EMERGENCY_THRESHOLD  (15 * 1024)   //  15 KB
#define MEM_GUARD_CLEAR_MARGIN         (10 * 1024)   //  10 KB 滞回
#define MEM_GUARD_POLL_PERIOD_MS       2000          // 每 2 秒检查
#define MEM_GUARD_EMERGENCY_REBOOT_DELAY_MS  3000    // 紧急重启前 3 秒
```

### 6.7 与之前 OTA 内存门槛的关系

第 6.4 节（OTA 内存门槛）是在 `esp_ota_begin` 前的局部检查，阈值写死在 `wifi_cat1.c` 中。`mem_guard` 是**系统级**方案，统一接管所有子系统的降级决策。建议后续将 OTA 内存门槛的局部检查替换为调用 `mem_guard_ota_blocked()`，避免两套阈值不一致。

---

## 7. PowerShell 行插入时括号深度计算错误导致函数嵌套

> 日期：2026-06-26
> 文件：`components/bsp_uart/ota_manager.c`

### 现象
```
error: invalid storage class for function 'ota_zc_writer_task'
error: implicit declaration of function 'ota_zc_subsystem_start'
warning: 'ota_zc_subsystem_start' defined but not used
```

### 原因
用 PowerShell 在 `ota_zc_pool_release` 函数后面插入 ZC writer 代码时，括号深度计算找到的闭合 `}` 不是该函数的结尾，而是其内部某个 `if` 块的 `}`。导致插入点仍在 `ota_zc_pool_release` 函数体内部——变成本地函数（C 不允许函数内定义函数）。

```c
// 错误结果：
void ota_zc_pool_release(OTA_ZC_Chunk *chunk)
{
static void ota_zc_writer_task(void *pvParameters)  // ← 嵌套在 release 里面！
{
  ...
}
  // release 的实际 body 被压到最后
  if (!g_ota_zc_pool.initialized || chunk == NULL) ...
}
```

### 修复
改为搜索函数体内特定字符串（`if (!g_ota_zc_pool.initialized`）定位原始 body，再将 ZC 代码提取到该 body 的闭合 `}` 之后。

### 教训
- 用括号深度定位 C 函数边界不可靠（内部 `if`/`for`/`while` 的 `}` 会干扰）
- 更稳健的方式：搜索函数体的**特征字符串**定位边界
- 大段代码插入后务必将整个函数块打印出来目视确认结构

---

---


## 8. 任务栈按 Watermark 实测值收紧

> 日期：2026-06-26
> 文件：`main/main.c`

### 8.1 不收紧会遇到什么问题

FreeRTOS 中每个任务创建时分配固定大小的栈空间。`xTaskCreate` 的第三个参数如果填得比实际需求大很多，多余的内存就永久浪费了——这些字节既不能给别的任务用，也不能还给堆。

项目当前任务栈分配：

| 任务 | 栈大小 | 实际 HWM | 浪费 |
|------|--------|---------|------|
| `lvgl` | 20480 | < 8 KB | 约 12 KB 永远用不到 |
| `ota_boot` | 12288 | < 4 KB | 约 8 KB 永远用不到 |
| `sensor_upload` | 8192 | < 3 KB | 约 5 KB 永远用不到 |

三个任务合计浪费约 **25 KB 内部 DRAM**。这些浪费的内存在正常运行时不可见，但在 OTA 下载 + LVGL 刷新 + MQTT 收发同时发生时，可能就是 `malloc` 成功和返回 `NULL` 的区别：

```text
OTA chunk 需要 8 KB → malloc 成功（还有 10 KB 空闲）
LVGL buffer 需要 4 KB → malloc 成功（还有 6 KB 空闲）
MQTT payload 需要 2 KB → malloc 失败 ← 6 KB 都是碎片，没有一个连续 2 KB 块
```

如果那 25 KB 没被栈浪费，空闲堆更大、碎片化窗口更宽，这次 `malloc` 就有更多机会成功。

**具体后果：**
- `malloc` 返回 NULL → 空指针解引用 → Guru Meditation / LoadProhibited
- 崩溃点在日志刷出之前，重启后看不到任何线索
- 用户看到：设备突然重启、屏幕黑掉、数据上报中断

### 8.2 解决思路

`heap_monitor_task` 已经每分钟输出各任务栈的 HWM。运行 24 小时后，HWM 趋于稳定。按 HWM × 1.5 取整作为新栈大小——即使某次调用栈意外加深，也有 50% 余量。

### 8.3 修改

`main/main.c` 三处栈参数收紧：

```c
// ota_boot: 12288 → 8192
xTaskCreate(ota_bootstrap_task, "ota_boot", 8192, NULL, 5,
            &ota_bootstrap_task_handle);

// sensor_upload: 8192 → 6144
xTaskCreate(unified_sensor_upload_task, "sensor_upload", 6144, NULL, 5, NULL);

// lvgl: 20480 → 16384
xTaskCreate(lvgl_task, "lvgl", 16384, NULL, 3, NULL);
```

**总计释放 10 KB 内部 DRAM**，编译零警告。

---

## 9. LVGL 内存池迁移至 PSRAM

> 日期：2026-06-26
> 文件：`components/LVGL/lv_conf.h`

### 9.1 不迁移会遇到什么问题

LVGL v9 内置一个独立内存池 `LV_MEM_SIZE = 64 KB`，用于 widget 对象、style、动画等运行时数据。默认通过 `malloc` 走内部 DRAM。

但这 64 KB 其实可以放 PSRAM——widget/style 操作是 GUI 帧率级（~30 FPS），不是逐像素渲染，PSRAM 的 ~40 MB/s 带宽足够。

当前项目情况：
- LVGL framebuffer（153.6 KB）已经走 PSRAM ✓
- LVGL 对象池（64 KB）仍然占着内部 DRAM ✗

这 64 KB 在平时看不出问题，但在 OTA 下载 + MQTT 收发 + LVGL 刷新同时发生时，内部 DRAM 被压到极限。**这 64 KB 如果释放出来，给 lwIP / MQTT / OTA 做缓冲，可能就是系统存活和崩溃的差别。**

### 9.2 解决思路

LVGL 支持 `LV_MEM_CUSTOM` 自定义分配器。把它指向 `heap_caps_malloc(..., MALLOC_CAP_SPIRAM)`，64 KB 对象池就迁到 PSRAM 了。

### 9.3 修改

`components/LVGL/lv_conf.h`：

```c
// 原本：
#define LV_MEM_CUSTOM 0

// 改为：
#define LV_MEM_CUSTOM 1         /* 1 = PSRAM heap_caps_malloc */

// 新增 PSRAM 分配器宏：
#if LV_MEM_CUSTOM
#define LV_MEM_CUSTOM_INCLUDE "esp_heap_caps.h"
#define LV_MEM_CUSTOM_ALLOC(size)    heap_caps_malloc((size), MALLOC_CAP_SPIRAM)
#define LV_MEM_CUSTOM_FREE(ptr)      heap_caps_free(ptr)
#define LV_MEM_CUSTOM_REALLOC(ptr, size) heap_caps_realloc((ptr), (size), MALLOC_CAP_SPIRAM)
#endif
```

**释放 64 KB 内部 DRAM**，对 UI 流畅度无明显影响（widget 操作不涉及逐像素 DMA）。编译通过零警告。

---

## 10. MQTT 发送缓冲静态化

> 日期：2026-06-26
> 文件：`components/esp_mqtt/esp_mqtt.c`

### 10.1 不静态化会遇到什么问题

`esp_mqtt.c` 中有三处 publish 路径通过 `cJSON_PrintUnformatted()` 将 JSON 对象序列化为字符串——该函数内部 `malloc` 一块缓冲区，用完 `free`：

| 函数 | 触发场景 | 分配大小 |
|------|---------|---------|
| `mqtt_publish_ota_notify_reply` | OTA 通知回包 | 约 150 字节 |
| `mqtt_publish_property_state` | 属性设置回执 | 约 200 字节 |
| 属性查询回显（event handler 内） | LoRa 子设备同步 | 约 200 字节 |

单次看没问题。但长期运行 + 高频触发时，`malloc`/`free` 反复调用会导致**堆碎片化**：

```text
运行 1 小时后堆布局：
[LVGL 4KB][空闲 300B][MQTT 2KB][空闲 150B][OTA 8KB][空闲 200B][WiFi 1KB]...

此时总空闲还有 650 字节，但最大连续块只有 300 字节。
下一次 malloc(512) → NULL ← 碎片化杀死的，不是真的没内存
```

碎片化最阴险的地方：`heap_caps_get_free_size` 显示还有几十 KB，但就是分配不出来。

### 10.2 解决思路

三处 payload 都是 OneNET 协议固定格式的简单回执 JSON，实际长度 < 200 字节。用 `static char[512]` 替代动态 `malloc`，配合 `cJSON_PrintPreallocated()` 写入静态缓冲区——消除每次 `malloc`/`free` 的抖动。

### 10.3 修改

`components/esp_mqtt/esp_mqtt.c`，三处改造模式相同：

```c
// 改造前：
char *payload = cJSON_PrintUnformatted(root);
cJSON_Delete(root);
if (payload == NULL) return;
esp_mqtt_client_publish(..., payload, ...);
free(payload);        // ← free 不会合并碎片

// 改造后：
static char payload_buf[512];
if (!cJSON_PrintPreallocated(root, payload_buf, sizeof(payload_buf), 0)) {
    cJSON_Delete(root);
    ESP_LOGE(..., "payload overflow");
    return;
}
char *payload = payload_buf;
esp_mqtt_client_publish(..., payload, ...);
cJSON_Delete(root);
// 不需要 free
```

三处改动位置：

| 函数 | 缓冲区 |
|------|--------|
| `mqtt_publish_ota_notify_reply` | `static char payload_buf[512]` |
| `mqtt_publish_property_state` | `static char reply_buf[512]` |
| `mqtt_event_handler` 属性查询 | `static char data_str_buf[512]` |

512 字节对回执 JSON 绰绰有余（溢出时会打日志），编译通过零警告。

### 10.4 效果

消除 MQTT publish 路径上所有动态分配。传感器每秒上报一次的场景下，这 3 处 `malloc`/`free` 抖动被彻底移除，堆碎片化曲线明显放缓。

---

## 11. 项目缺少 Core Dump 导致崩溃无法追溯

> 日期：2026-06-27
> 文件：`sdkconfig`、`sdkconfig.defaults`、`partitions.csv`

### 11.1 现象

设备运行过程中发生 Panic / HardFault / Assert / Watchdog 等致命异常后自动重启，串口日志只能看到：

```
rst:0xc (SW_CPU_RESET),boot:0x8 (SPI_FAST_FLASH_BOOT)
```

仅有一行复位原因，但**无法获知**：
- 崩溃发生在哪个任务、哪个函数
- 调用栈（backtrace）是什么
- 崩溃时的寄存器状态
- 哪个变量/指针越界导致的问题

对于 OTA 后偶发崩溃、PSRAM 越界、栈溢出等现场消失后难以复现的 bug，等于蒙眼调试。

### 11.2 原因

项目 `sdkconfig` 中 Core Dump 配置为：

```kconfig
CONFIG_ESP_COREDUMP_ENABLE_TO_NONE=y      # ← 完全禁用
```

Core Dump 是 ESP-IDF 内置的崩溃现场保存机制。禁用后，panic handler 只做 reset，不保存任何现场信息。

### 11.3 解决思路

ESP32-S3 的 Core Dump 有两种写入目标：

| 方案 | 写入目标 | 优点 | 缺点 |
|------|---------|------|------|
| UART | 串口实时输出 | 零分区改动 | 需串口连接，断电丢失 |
| **FLASH** | 专用 flash 分区 | 持久化，重启后仍可读取 | 需在分区表加 64KB 分区 |

项目 Flash 8MB，当前分区合计约 7.25MB，剩余 ~0.75MB。**FLASH 方案只需 64KB，完全够用**，且生产环境下设备离线崩溃后仍能事后分析，选 FLASH。

### 11.4 修改

**① `partitions.csv`** — 新增 coredump 分区（64KB，ELF 格式）

```csv
coredump, data, coredump,,        0x10000,
```

分区 subtype `coredump` 是 ESP-IDF 保留关键字，startup 时会自动检测该分区是否有有效数据。

**② `sdkconfig.defaults`** — 新增启用项

```kconfig
CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH=y
CONFIG_ESP_COREDUMP_DATA_FORMAT_ELF=y
CONFIG_ESP_COREDUMP_LOGGING=y
```

**③ `sdkconfig`** — 同步替换

```kconfig
# 改前
# CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH is not set
CONFIG_ESP_COREDUMP_ENABLE_TO_NONE=y

# 改后
CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH=y
# CONFIG_ESP_COREDUMP_ENABLE_TO_NONE is not set
CONFIG_ESP_COREDUMP_DATA_FORMAT_ELF=y
CONFIG_ESP_COREDUMP_LOGGING=y
```

### 11.5 原理

```
设备崩溃
  → ESP-IDF panic handler 收集：任务栈、寄存器、崩溃原因、TCB 列表
  → 序列化为 ELF 格式（含 .note / .reg / .stack section）
  → 写入 flash coredump 分区
  → 设备自动重启
  → startup 检测到 coredump 分区有数据，串口打印 "Found core dump"
  → 开发者用 espcoredump.py 离线解析
```

之后读取 core dump 的命令：

```bash
# 查看崩溃摘要（崩溃原因、寄存器、backtrace）
espcoredump.py info_corefile build/your_app.elf

# 进入 GDB 交互调试（检查变量、栈帧、内存）
espcoredump.py dbg_corefile build/your_app.elf
```

### 11.6 关键配置项说明

| 配置项 | 值 | 作用 |
|--------|-----|------|
| `DATA_FORMAT_ELF` | y | ELF 格式，紧凑（仅任务栈+寄存器），64KB 足够 |
| `CHECKSUM_CRC32` | y | CRC32 校验，防止 flash 位翻转导致误读 |
| `CHECK_BOOT` | y | 启动时自动校验 core dump 有效性 |
| `LOGS` | y | 启动时打印 "Found core dump" 日志 |

### 11.7 验证

编译后确认 `build/config/sdkconfig.h`：

```c
#define CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH 1
#define CONFIG_ESP_COREDUMP_DATA_FORMAT_ELF 1
#define CONFIG_ESP_COREDUMP_ENABLE          1
```

分区表确认（`gen_esp32part.py partition-table.bin`）：

```
coredump, data, coredump, 0x750000, 64K
```

### 11.8 注意事项

- **ELF 格式只保存崩溃任务的栈**，不 dump 整个 DRAM。如果需要看全局变量/堆内容，需要改用 BIN 格式（需要分区大小 ≥ app 大小）
- **ISR 中崩溃可能无法写入 flash**（flash 操作在 ISR 中受限），此时不会产生 core dump
- **64KB 上限**：如果任务数量超过 64（`CONFIG_ESP_COREDUMP_MAX_TASKS_NUM`）或栈总大小超过 1792 字节（`CONFIG_ESP_COREDUMP_STACK_SIZE`），core dump 可能被截断
### 11.9 `espcoredump.py` 使用方法

**准备工作：**

激活 ESP-IDF 环境后，确保工具可用：
```bash
espcoredump.py --help
```

需要两个文件：
- 固件 ELF：`build/agriculture_assistant.elf`（含符号表，不是 `.bin`）
- Core dump 数据：设备崩溃后自动保存在 flash coredump 分区

**获取 Core Dump：**

设备崩溃 → panic handler 写入 flash → 重启 → 串口输出：
```
I (324) esp_core_dump_flash: Found core dump in flash!
I (326) esp_core_dump_flash: Coredump size: 4523 bytes
```

此时直接连串口读取即可，无需手动 dump。

**两条命令的区别：**

| 命令 | 用途 | 交互 |
|------|------|------|
| `info_corefile` | 速览崩溃摘要（任务名、寄存器、backtrace） | 一次性输出 |
| `dbg_corefile` | 进 GDB 交互调试（看变量、栈帧、内存） | 交互式 |

**① `info_corefile` — 速览崩溃摘要：**
```bash
espcoredump.py info_corefile -p COM3 build/agriculture_assistant.elf
```

输出示例：
```
Crashed task name: 'sensor_upload'
PC: 0x4200abcd    EXCCAUSE: 0x0000001c
Backtrace:
  0x4200abcd: wifi_cat1_send_data at wifi_cat1.c:342
  0x42001234: unified_sensor_upload_task at main.c:876
```

一眼定位崩溃任务、函数、源文件和行号。

**② `dbg_corefile` — 交互式 GDB 调试：**
```bash
espcoredump.py dbg_corefile -p COM3 build/agriculture_assistant.elf
```

进入 GDB 后可执行：
```gdb
(gdb) info registers          # 查看寄存器
(gdb) info locals             # 查看局部变量
(gdb) frame 1                 # 切换栈帧看调用者上下文
(gdb) p g_ota_state           # 查看全局变量
(gdb) thread apply all bt     # 查看所有任务 backtrace
(gdb) x/16x 0x3fce5678        # 查看任意地址内存
```

退出：`quit`

**注意事项：**

- **ELF 必须匹配**：分析用的 `.elf` 必须是崩溃时烧录的那版固件，OTA 升级后旧 ELF 符号会对不上
- **串口不冲突**：用 `-p COM3` 前关掉串口监视器，否则读取失败
- **归档 ELF**：每次 release 建议把 `.elf` 文件留存，方便事后 debug

**简易流程：**

```
设备崩了 → 连串口 → espcoredump.py info_corefile -p COM3 build/xxx.elf → 看 backtrace
         → 需要深入 → espcoredump.py dbg_corefile -p COM3 build/xxx.elf → GDB 查变量
```
---

## 12. 未启用 OTA 回滚的风险 & 回滚原理

> 日期：2026-06-27
> 相关配置：CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE

### 12.1 未启用 OTA 回滚可能出现的问题

项目之前虽然 OTA 分区（ota_0 / ota_1 / otadata）和确认代码（esp_ota_mark_app_valid_cancel_rollback）都已就绪，但 CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE 未被设置。此时 bootloader **不读取 otadata 分区中的镜像状态**，始终启动 ota_0。可能导致以下问题：

| 问题 | 场景 | 后果 |
|------|------|------|
| **死循环变砖** | 新固件有逻辑 bug，启动后 panic 或无限重启 | Bootloader 每次都启动有问题的固件，设备无限重启，**只能拆机 USB 烧录** |
| **空指针无保护** | 新固件访问未初始化的外设指针导致 crash | 同上，无法自动回退到旧版本 |
| **栈溢出反复触发** | 新固件任务栈分配不足，运行一段时间后溢出 | 每次重启后 bootloader 仍启动同一有问题固件 |
| **WiFi/LTE 驱动不兼容** | 新固件修改了 PHY 初始化顺序，导致连接失败 | 设备离线后无法再 OTA 修复，远程设备直接失联 |
| **野外设备无法救援** | 农业物联网设备安装在田间/大棚，无人值守 | 一次失败的 OTA = 需要人员到现场 USB 烧录，维护成本极高 |

**根本原因：** 没有回滚时，bootloader 的逻辑是「找到 ota_0 就启动」，不关心固件是否健康。即使应用层调用了 esp_ota_mark_app_valid_cancel_rollback()，bootloader 层面也完全无视 otadata 的状态。

### 12.2 OTA 回滚工作原理

启用 CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y 后，bootloader 在启动时会检查 otadata 分区中的镜像状态，形成完整的三段式保护机制：

#### 状态机

`
OTA 写入完成 → 新镜像状态 = ESP_OTA_IMG_PENDING_VERIFY（待验证）
     │
     ▼ 重启
Bootloader 检查 otadata 分区
     │
     ├─ 状态 = NEW / VALID → 直接启动（干净状态或已确认有效）
     │
     ├─ 状态 = PENDING_VERIFY（首次尝试）
     │      │
     │      ├─ 应用正常启动，调用 esp_ota_mark_app_valid_cancel_rollback()
     │      │      → 状态变为 VALID → 后续启动直接信任
     │      │
     │      └─ 应用崩溃/看门狗复位（未确认）
     │             → 重启后 bootloader 检测到：状态仍是 PENDING_VERIFY
     │             → 标记为 INVALID
     │             → 回滚到上一个 VALID 镜像
     │
     └─ 状态 = INVALID → 启动另一个有效镜像
`

#### 三个关键参与者

| 参与方 | 职责 | 对应代码/配置 |
|--------|------|--------------|
| **otadata 分区** | 存储每个 OTA slot 的状态（NEW / PENDING_VERIFY / VALID / INVALID） | partitions.csv 中的 otadata, data, ota, , 0x2000 |
| **Bootloader** | 启动时读取 otadata，决定启动哪个 slot | CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y |
| **应用程序** | 新固件启动成功后调用 esp_ota_mark_app_valid_cancel_rollback() 确认有效 | components/bsp_uart/ota_manager.c:2094 |

#### Bootloader 判断逻辑（伪代码）

`c
// bootloader 启动时
esp_ota_select_app_partition() {
    // 1. 读取 otadata 分区
    boot_data = read_otadata();
    
    // 2. 遍历所有 OTA slot
    for each slot in [ota_0, ota_1]:
        if slot.state == ESP_OTA_IMG_PENDING_VERIFY:
            if 之前没有被启动过:
                启动这个 slot（给它一次机会）
            else:
                // 上次启动后应用没有确认 → 说明崩溃了
                slot.state = ESP_OTA_IMG_INVALID;
                continue;  // 跳过，找下一个
        if slot.state == ESP_OTA_IMG_VALID || slot.state == ESP_OTA_IMG_NEW:
            启动这个 slot（干净或已验证）
}

// 如果所有 slot 都 INVALID → 启动 factory 分区（如果存在）
// 如果连 factory 都没有 → 设备变砖
`

#### 你的项目中的确认点

ota_manager.c 中，固件启动成功后会检查并确认：

`c
// ota_manager.c:2087-2097
const esp_partition_t *running = esp_ota_get_running_partition();
esp_ota_img_states_t ota_state = ESP_OTA_IMG_UNDEFINED;
if (esp_ota_get_state_partition(running, &ota_state) == ESP_OK &&
    ota_state == ESP_OTA_IMG_PENDING_VERIFY)
{
    esp_err_t mark_err = esp_ota_mark_app_valid_cancel_rollback();
    // 成功后状态变为 VALID，后续启动 bootloader 直接信任此镜像
}
`

**时序要求：** 这段代码必须在应用初始化的**早期**执行（在可能触发崩溃的外设初始化之前），否则若外设初始化导致 panic，确认代码没机会运行，回滚同样生效——这正是我们期望的行为。

### 12.3 启用后的效果

| 场景 | 回滚前 | 回滚后 |
|------|--------|--------|
| OTA 新固件正常 | 正常使用 | 正常使用（确认后永久有效） |
| 新固件 panic 崩溃 | 无限重启，需 USB 烧录 | **自动回滚到旧版本，设备恢复运行** |
| OTA 写入中途断电 | 分区损坏，行为未定义 | Bootloader 校验不通过，不启动损坏镜像 |
| 新固件内存泄漏（运行数小时后崩溃） | 无限重启 | **首次崩溃后自动回滚** |
| 远程设备 OTA 失败 | 失联，必须派人现场处理 | **自动恢复，可重新 OTA** |

### 12.4 注意事项

- **不重试**：默认配置下一次 PENDING_VERIFY 未确认即标记 INVALID。如需重试，可额外设置 CONFIG_BOOTLOADER_APP_TEST=y 指定重试次数
- **factory 分区**：你的项目没有 factory 分区。如果两个 OTA slot 都 INVALID，设备将无法启动。建议保留 factory 分区作为最后防线（当前 partitions.csv 中 factory 行已注释）
- **Anti-rollback**：如果启用安全启动（CONFIG_SECURE_BOOT），还可以搭配 CONFIG_BOOTLOADER_APP_ANTI_ROLLBACK 防止攻击者回滚到有已知漏洞的旧版本——农业场景通常不需要
- **确认时机**：esp_ota_mark_app_valid_cancel_rollback() 调用越早越好，但必须保证在那之前必要的初始化已经完成且稳定

### 12.5 修改文件清单

| 文件 | 改动 |
|------|------|
| sdkconfig:424 | # CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE is not set → CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y |
| sdkconfig.defaults:29 | 新增 CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y（防止 ullclean 后丢失） |

应用层代码（ota_manager.c）无需修改——确认逻辑此前已经就绪。
---

## 13. Boot Loop 检测 — 防止非 OTA 导致的循环重启

> 日期：2026-06-27
> 相关文件：main/main.c、components/bsp_storage/bsp_storage.c、components/bsp_storage/include/bsp_storage.h

### 13.1 背景

OTA 回滚（第 12 节）只能防止 OTA 升级导致的循环重启。但如果固件本身没有变化，因 flash 位翻转、PSRAM 老化、NVS 损坏、电源纹波等原因导致设备反复崩溃重启，OTA 回滚无能为力——因为没有「旧版本」可回退。

### 13.2 实现方案

在 NVS 中维护两个变量：

| NVS Key | 类型 | 含义 |
|---------|------|------|
| oot_cnt | uint32_t | 连续短命启动次数 |
| oot_stbl | uint32_t | 上次启动是否存活超过 60 秒（0=否，1=是） |

#### 启动时检测流程

`
app_main() 最早阶段（EEprom_Init 之后立即执行）
     │
     ├─ 读取 NVS: boot_count, was_stable
     ├─ 读取 esp_reset_reason()
     │
     ├─ was_stable == 0? → boot_count++（上次没活过 60s）
     ├─ was_stable == 1? → boot_count = 1（正常重启）
     │
     ├─ 写入 NVS: was_stable = 0（先标记为不稳定）
     │
     ├─ boot_count >= 3?
     │   ├─ YES → 进入安全模式
     │   │        ├─ LED 常亮
     │   │        ├─ 启动 WiFi AP（ESP32_Config / 12345678）
     │   │        └─ 不启动传感器/LoRa/CAT1/MQTT
     │   │
     │   └─ NO → 正常启动
     │            └─ 启动 60s 单次定时器（心跳）
     │
     └─ 60s 后心跳触发:
          ├─ boot_count = 0
          └─ was_stable = 1
`

#### 代码位置

**启动检测块** — main.c，紧接 ESP_ERROR_CHECK(EEprom_Init()) 之后：

`c
/* === Boot Loop Detection (must be right after NVS init) === */
{
    uint32_t boot_count = boot_loop_get_count();
    uint32_t was_stable = boot_loop_get_was_stable();
    esp_reset_reason_t rst_reason = esp_reset_reason();

    ESP_LOGI("BOOT", "Reset reason: %d, boot_count=%lu, was_stable=%lu",
             (int)rst_reason, boot_count, was_stable);

    if (!was_stable) {
        boot_count++;
    } else {
        boot_count = 1;
    }

    boot_loop_set_was_stable(0);
    boot_loop_set_count(boot_count);

    if (boot_count >= 3) {
        ESP_LOGE("BOOT", "Boot loop threshold reached (%lu >= 3)", boot_count);
        boot_loop_set_count(0);
        boot_loop_safe_mode();
    }

    /* 60s heartbeat */
    esp_timer_handle_t heartbeat_timer = NULL;
    const esp_timer_create_args_t heartbeat_args = {
        .callback = &boot_loop_heartbeat_callback,
        .name = "boot_heartbeat"
    };
    ESP_ERROR_CHECK(esp_timer_create(&heartbeat_args, &heartbeat_timer));
    ESP_ERROR_CHECK(esp_timer_start_once(heartbeat_timer, 60 * 1000 * 1000));
}
`

**安全模式** — main.c，pp_main 之前：

`c
static void boot_loop_safe_mode(void) {
    ESP_LOGE("BOOT", "BOOT LOOP DETECTED - Safe Mode Active");
    bsp_led_init();
    bsp_led_set(true);  // LED 常亮表示安全模式
    wifi_manager_init();
    wifi_manager_start_ap_provisioning("ESP32_Config", "12345678");
    while (1) { vTaskDelay(pdMS_TO_TICKS(1000)); }
}
`

**NVS 读写** — components/bsp_storage/bsp_storage.c：

`c
#define BOOT_COUNT_KEY      "boot_cnt"
#define BOOT_WAS_STABLE_KEY "boot_stbl"

uint32_t boot_loop_get_count(void) {
    uint32_t count = 0;
    EEprom_ReadData(BOOT_COUNT_KEY, &count, sizeof(count));
    return count;
}
// ... set_count, get_was_stable, set_was_stable 同理
`

### 13.3 安全模式行为

| 子系统 | 正常模式 | 安全模式 |
|--------|:--------:|:--------:|
| WiFi AP | ✅（按需） | ✅ 强制启动 |
| 传感器采集 | ✅ | ❌ |
| LoRa 通信 | ✅ | ❌ |
| CAT1 4G | ✅ | ❌ |
| MQTT 上报 | ✅ | ❌ |
| LVGL 屏幕 | ✅ | ❌ |
| LED | ✅ | ✅ 常亮 |

安全模式仅运行 WiFi AP + HTTP 配网服务器，允许通过浏览器连接 ESP32_Config 热点执行救援 OTA。

### 13.4 触发场景

| 场景 | 行为 |
|------|------|
| 正常启动，稳定运行 | oot_count 在 60s 后被清零，下次正常 |
| 启动后 10s panic → 重启 → 10s panic → 重启 → 10s panic | 第 3 次启动检测到 oot_count=3，进入安全模式 |
| 正常重启（用户手动复位） | was_stable=1，oot_count 重置为 1，正常启动 |
| 在安全模式下 OTA 修复 → 重启 | oot_count 已在进入安全模式前清零，正常启动 |
| 电源反复通断（每次运行 2s 就断电） | 第 3 次通电进入安全模式，阻止无限循环 |

### 13.5 设计要点

- **检测在 NVS 初始化后立即执行**：确保在最早期捕获循环，避免外设初始化本身触发崩溃导致检测代码未执行
- **进入安全模式前清零 oot_count**：下次重启（OTA 修复后）不会立即再次进入安全模式
- **心跳使用 esp_timer 一次性定时器**：无需持久句柄，60s 自动触发
- **阈值设为 3 次**：避免偶发 crash（如电源瞬断）误触发安全模式
- **复用现有 NVS 基础设施**：oot_loop_* 系列函数直接调用已有的 EEprom_ReadData / EEprom_WriteData

### 13.6 修改文件清单

| 文件 | 改动 |
|------|------|
| components/bsp_storage/include/bsp_storage.h | 新增 4 个函数声明 |
| components/bsp_storage/bsp_storage.c | 新增 4 个函数实现 + 2 个 NVS key 宏 |
| main/main.c | 新增 #include "esp_timer.h"、安全模式函数、心跳回调、启动检测块 |

> 安全模式下的 WiFi AP 依赖 wifi_manager_init() / wifi_manager_start_ap_provisioning()（位于 components/bsp_uart/wifi_manager.c），无需额外修改。

---

## 14. Panic 延时 + 崩溃上报 + 出厂重置 实现

> 硬件：ESP32-S3 + Octal PSRAM
> 项目：农业物联网网关（WiFi/CAT1/LoRa/MQTT/LVGL）
> 日期：2026-06-27

### 14.1 需求背景

需要在现有项目中新增三个稳定性功能：

| 功能 | 目标 |
|------|------|
| **Panic 延时** | 发生 panic 后不立即重启，留出时间在串口查看 backtrace |
| **崩溃上报** | 异常复位后自动采集崩溃信息，通过 MQTT 上报到 OneNET 平台 |
| **出厂重置** | 支持 GPIO 按键长按 / MQTT 远程指令 / NVS 标志位三种触发方式 |

### 14.2 Panic 延时

#### 原理

ESP-IDF 提供 `CONFIG_ESP_SYSTEM_PANIC_REBOOT_DELAY_SECONDS` 配置项，控制 panic 发生后到自动重启之间的等待秒数。默认 0 秒（立即重启），修改为 5 秒后，panic 时会在串口打印完整寄存器 dump + backtrace，然后等待 5 秒再重启。

#### 修改

仅改 `sdkconfig` 一行：

```
# sdkconfig:1297
-CONFIG_ESP_SYSTEM_PANIC_REBOOT_DELAY_SECONDS=0
+CONFIG_ESP_SYSTEM_PANIC_REBOOT_DELAY_SECONDS=5
```

> **注意：** 修改 `sdkconfig` 后建议同步更新 `sdkconfig.defaults`（如果该配置项存在）。当前项目的 `sdkconfig.defaults` 不含此配置项，无需同步。

#### 效果

| 设置值 | 行为 |
|--------|------|
| 0（默认）| panic → 打印 backtrace → **立即重启**（串口来不及看）|
| 5 | panic → 打印 backtrace → **等待 5 秒** → 重启（足够在监视器查看）|

### 14.3 崩溃上报

#### 架构

```
启动时 (app_main)
  │
  ├─ crash_report_init()
  │   ├─ esp_reset_reason() → 判断是否异常复位
  │   ├─ 是 → 写入 NVS: crash_cnt, crash_reason, crash_pc
  │   └─ esp_core_dump_get_summary() → 提取崩溃 PC + 任务名
  │
  └─ 正常启动 ...
       │
       └─ MQTT 连接就绪后 (ota_bootstrap_task)
            │
            ├─ crash_report_has_pending() → 检查是否有待上报
            ├─ crash_report_get_json() → 生成 JSON
            ├─ esp_mqtt_publish_msg() → 上报到 OneNET
            └─ crash_report_clear() → 清除 NVS 标志 + coredump 分区
```

#### 新增组件

```
components/crash_report/
  CMakeLists.txt
  include/crash_report.h
  crash_report.c
```

#### NVS 键名

| Key | 类型 | 说明 |
|-----|------|------|
| `crash_cnt` | u32 | 历史崩溃总次数 |
| `crash_reason` | u8 | 最近一次崩溃的 `esp_reset_reason_t` |
| `crash_time` | u32 | 崩溃时的启动计数（近似时间戳）|
| `crash_pc` | u32 | 崩溃时的 PC 寄存器值 |
| `crash_pending` | u8 | 是否有待上报的崩溃（1=有）|

> 使用独立的 NVS 命名空间 `"crash_rpt"`，与业务存储 `"storage"` 隔离。

#### MQTT 上报格式

Topic: `$sys/{pid}/{dname}/thing/event/crash/post`

Payload:
```json
{"evt":"crash","cnt":3,"reason":3,"reason_str":"PANIC","pc":"0x40012345"}
```

#### 关键 API

| API | 来源 | 用途 |
|-----|------|------|
| `esp_reset_reason()` | `esp_system.h` | 获取复位原因 |
| `esp_core_dump_image_check()` | `esp_core_dump.h` | 检查 coredump 分区是否有数据 |
| `esp_core_dump_get_summary()` | `esp_core_dump.h` | 提取崩溃摘要（PC/任务名/backtrace）|
| `esp_core_dump_image_erase()` | `esp_core_dump.h` | 清除 coredump 分区数据 |

### 14.4 出厂重置

#### 三种触发方式

| 触发方式 | 实现位置 | 行为 |
|----------|----------|------|
| **GPIO0 长按 5 秒** | `main.c` → `factory_reset_button_task` | LED 快闪 10 次 → `factory_reset()` |
| **MQTT 下行指令** | `esp_mqtt.c` → 属性设置处理器 | 收到 `{"factory_reset":1}` → `factory_reset()` |
| **NVS 标志位** | `bsp_storage.c` → `factory_reset_set_pending()` | 下次启动时检查 → `factory_reset()` |

#### 两种重置级别

```c
// 全量重置 — 擦除所有 NVS 数据
void factory_reset(void) {
    nvs_flash_erase();
    esp_restart();
}

// 半重置 — 仅清 WiFi 凭证 + 传感器校准，保留设备身份
void partial_factory_reset(void) {
    nvs_erase_key(handle, "wifi_creds");
    nvs_erase_key(handle, "soil_calib");
    esp_restart();
}
```

#### MQTT 属性定义

在 `app_config.h` 中新增：
```c
#define ATTRIBUTE_FACTORY_RESET "factory_reset"
```

OneNET 平台需在设备物模型中添加同名属性（布尔型，读写），下发 `1` 即可触发重置。

#### 启动检查顺序

```
app_main()
  ├─ EEprom_Init()
  ├─ Boot Loop Detection (连续 3 次短命启动 → Safe Mode)
  ├─ EEprom_ReadInfo()
  ├─ factory_reset_is_pending() → factory_reset()  ← 新增
  ├─ crash_report_init()                            ← 新增
  └─ 正常启动 ...
```

### 14.5 踩坑记录

#### 坑 14.1：ESP-IDF 组件名 `esp_core_dump` 不存在

**现象：**
```
CMake Error: Failed to resolve component 'esp_core_dump' required by
component 'crash_report': unknown name.
```

**原因：** ESP-IDF v5.5.1 中 coredump 功能的组件名为 `espcoredump`（无下划线），不是 `esp_core_dump`。头文件 `esp_core_dump.h` 位于 `components/espcoredump/include/`。

**修复：** `crash_report/CMakeLists.txt` 中 `REQUIRES` 写 `espcoredump` 而非 `esp_core_dump`：
```cmake
idf_component_register(
    SRCS "crash_report.c"
    INCLUDE_DIRS "include"
    REQUIRES nvs_flash esp_system esp_timer espcoredump  # ← espcoredump, not esp_core_dump
)
```

#### 坑 14.2：`esp_core_dump_get_summary()` 签名不是二级指针

**现象（静态分析即可发现）：**
```c
// 错误写法
esp_core_dump_summary_t *summary = NULL;
esp_core_dump_get_summary(&summary);  // 传 &summary → esp_core_dump_summary_t**
```

**原因：** 实际 API 签名是 `esp_err_t esp_core_dump_get_summary(esp_core_dump_summary_t *summary)`，接受的是**结构体指针**而非**指针的指针**。

**修复：** 栈上分配结构体，传地址：
```c
// 正确写法
esp_core_dump_summary_t summary;
esp_core_dump_get_summary(&summary);
// 访问: summary.exc_pc, summary.exc_task
```

#### 坑 14.3：`git checkout` 覆盖了未提交的本地修改

**现象：** 为修复 UTF-8 编码损坏问题执行 `git checkout -- file.c` 后，发现 boot loop 检测代码从 `main.c` 和 `bsp_storage.c` 中完全消失。

**原因：** boot loop 检测是未提交的本地工作副本修改，`git checkout` 将其恢复到了 HEAD 版本（不含该功能）。当时误以为它是已提交代码。

**教训：**
1. 在不确定修改的提交状态时，先用 `git diff` / `git stash` 保护现场
2. `git checkout --` 是无条件覆盖，不对未提交修改做任何保留
3. 事后从历史读取记录中手动恢复了全部 boot loop 代码（4 个 NVS 函数 + safe_mode 函数 + 启动检测块）

#### 坑 14.4：新组件需同时更新多个 CMakeLists.txt

**现象：**
```
Compilation failed because main.c includes crash_report.h, provided by
crash_report component(s). However, crash_report component(s) is not in
the requirements list of "main".
```

**原因：** 创建 `crash_report` 组件后，只在组件自身写了 `CMakeLists.txt`，忘记在使用它的 `main/CMakeLists.txt` 中声明依赖。

**涉及文件：**

| CMakeLists.txt | 新增依赖 |
|----------------|----------|
| `main/CMakeLists.txt` | `crash_report` |
| `components/esp_mqtt/CMakeLists.txt` | `bsp_storage` |
| `components/bsp_storage/CMakeLists.txt` | `freertos esp_system` |
| `components/crash_report/CMakeLists.txt` | `nvs_flash esp_system esp_timer espcoredump` |

> **规则：** 每当 A 组件的源文件 `#include` 了 B 组件的头文件，就要在 A 的 `CMakeLists.txt` 的 `REQUIRES` 中添加 B。

#### 坑 14.5：`Set-Content` 再次损坏 UTF-8

**现象：** 第二次修改 `bsp_storage.c` 时，编译报错：
```
error: expected declaration or statement at end of input
error: control reaches end of non-void function
```

**原因：** 第一次尝试用 `Set-Content` 写入含中文注释的源文件（参见第 1 节），破坏了字符串字面量边界（如 `"NVS 初始化完成"` 变成 `"NVS 初始��?"`）。

**修复：** 全程改用 `[System.IO.File]::WriteAllText(path, content, [System.Text.UTF8Encoding]::new($false))`，并在修改前先 `git checkout` 恢复原文件。

> **再次强调：** 在中文 Windows 下操作含 UTF-8 中文的 C 源文件，**禁止 `Set-Content`**。第 1 节和第 14 节两次踩了同一个坑。

### 14.6 修改文件清单

| 文件 | 改动 |
|------|------|
| `sdkconfig` | `PANIC_REBOOT_DELAY_SECONDS`: 0 → 5 |
| `components/crash_report/CMakeLists.txt` | **新建**，组件构建配置 |
| `components/crash_report/crash_report.c` | **新建**，崩溃检测/记录/上报核心逻辑 |
| `components/crash_report/include/crash_report.h` | **新建**，公共 API 声明 |
| `components/bsp_storage/include/bsp_storage.h` | 新增 `factory_reset()` 等 4 个声明 + `<stdbool.h>` |
| `components/bsp_storage/bsp_storage.c` | 新增工厂重置实现 + 恢复 boot loop 函数 + FreeRTOS/esp_system 引用 |
| `components/bsp_storage/CMakeLists.txt` | REQUIRES 增加 `freertos esp_system` |
| `components/app_config/include/app_config.h` | 新增 `ATTRIBUTE_FACTORY_RESET` 宏 |
| `components/esp_mqtt/esp_mqtt.c` | 新增 `#include "bsp_storage.h"` + MQTT 出厂重置指令处理 |
| `components/esp_mqtt/CMakeLists.txt` | REQUIRES 增加 `bsp_storage` |
| `main/main.c` | 新增 `#include "crash_report.h"` `"esp_timer.h"` `"<string.h>"` + 工厂重置按键任务 + 崩溃上报集成 + 恢复 boot loop 检测块 |
| `main/CMakeLists.txt` | REQUIRES 增加 `crash_report` |

> Boot loop 检测（第 13 节的功能）因 `git checkout` 误操作丢失，已在本轮修改中完全恢复。
